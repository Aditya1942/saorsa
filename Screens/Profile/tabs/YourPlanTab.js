import React from 'react';
import {ScrollView, View} from 'react-native';

export const YourPlanTab = () => {
  return (
    <ScrollView
      style={{
        backgroundColor: '#fff',
        padding: 10,
        borderRadius: 25,
      }}></ScrollView>
  );
};
